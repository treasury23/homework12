{% extends "::base.html.twig" %}

{% block body %}
{% for item in items %}
    <div class="title">{{ item.title }}</div>
    <div class="img_float"><img src="{{ item.image.path }}"></div>

    <div>{{ item.body }}</div>

{% endfor %}

{% endblock body %}
